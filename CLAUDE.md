# プロジェクト概要

このリポジトリは、Rust で学習用のパストレーサーを作るためのプロジェクトです。
目的は、CPU で動くシンプルな実装から始めて、最終的には Vulkan のハードウェアレイトレーシングを使う GPU パストレーサーや、より先端的なレンダリング手法まで試せる土台を作ることです。

商用レンダラーではありません。
エンタープライズ向けの過剰な抽象化や厳密すぎるエラーハンドリングよりも、読みやすさ、デバッグしやすさ、挙動の理解しやすさを優先してください。

# 現在の前提

- 実装言語は Rust。
- 初期段階は CPU パストレーサーを優先し、動作確認とデバッグのしやすさを重視する。
- CPU 側では `glam`、`image`、`rayon`、`gltf` などを使う前提でよい。
- GPU 側では `vulkanalia` などの Rust 向け Vulkan バインディングを使う前提でよい。
- 将来的には高度なマテリアル、各種サンプリング、デノイザ、GPU パストレーシング、ボリューム、スペクトラル、ReSTIR 系や Path Guiding 系などへ広がる可能性がある。

詳細な実装順序やタスク一覧は別管理とし、このファイルには書きすぎないこと。

# 設計方針

- まずは単純で読みやすい実装を選ぶ。
- 将来の予定から見て本当に変わりそうな場所だけを柔軟にしておく。
- 早すぎる一般化、プラグイン機構、過度な trait 分割、DI 的な構成は不要。
- いま必要な具体実装で十分なら、そのまま素直に書いてよい。
- 次の段階で形が変わることが明らかな箇所だけ、小さな境界を作って分離する。

# 変更に開いておくべき箇所

- シーン表現の境界。ジオメトリ、インスタンス、変換、カメラ、シーントラバーサルは 1 つの実装詳細に強く結びつけすぎない。
- アクセラレーション構造。CPU の BVH と Vulkan の BLAS/TLAS は別物なので、構築や走査の都合は局所化する。
- マテリアルと BSDF 表現。Diffuse、Mirror、Glass、GGX、レイヤード材質、Disney BRDF、Standard Surface、OpenPBR、MaterialX 系の拡張が入りやすい。
- ライトサンプリングと light transport。NEE、MIS、環境光サンプリング、ManyLight、ReSTIR 系、Path Guiding、コースティクス改善手法などで構造が変わりやすい。
- テクスチャやマテリアル入力の層。初期は glTF 中心でも、将来的にはより広いアセット表現に接続される可能性が高い。
- 色表現。将来スペクトラル対応に進む余地を残す。
- デノイザやアップスケーラの統合点。複数手法を差し替えて比較しやすいとよい。
- デバッグ可視化の差し込み点。レイの反射経路やサンプリング挙動の確認を入れやすくする。

# ベタ書きでもよい箇所

- アプリケーション起動処理、CLI、細かなユーティリティ。
- 画像出力、ファイル書き出し、単純なシーン組み立て補助。
- その時点の学習や検証にだけ使う単機能なデバッグコード。
- アーキテクチャ境界になっていない小さな数学・補助関数。
- 一時的なハードコード値、テストシーン、検証用の分岐。

# 実装時の感覚

- 共通概念は、まず trait ではなく素直な struct / enum で持つ。
- 賢さより、追いやすいデータフローと制御フローを優先する。
- 少し再利用しやすいことより、デバッガで追いやすいことのほうが大事な場面が多い。
- 次の実装段階で実際に困るまでは、先回りしたリファクタリングをしない。

# このプロジェクトで目指す状態

各段階で、挙動を観察しやすく、画像で検証しやすく、次のレンダリング機能を追加するための最小限の拡張余地だけを持ったコードベースであること。

# テスト方針

- 新機能を実装するときは、必ずテストを実装できるか検討すること。実装可能なテストは必ず書く。
- 数学ユーティリティ、BSDF の評価・サンプリング、サンプリングの分布、座標変換、BVH 構築・走査などは単体テストで挙動を固定しやすい領域なので、優先的にテストを書く。
- 画像全体の再現のような結合的な検証は、`result/` 以下への出力と目視確認で補う形でよいが、そこに至る途中の計算は単体テストで抑えておく。
- テストを書くのが不自然・困難な箇所(たとえば GPU 側のレンダリング結果全体、対話的なデバッグ用コードなど)は、その理由を簡潔に説明したうえでスキップしてよい。

# 画像出力ルール

- テストや動作確認で画像を描画するときは、生成物を `result/` 以下に保存する。
- 生成画像は Git 管理対象にしない前提で扱う。

# ドキュメント運用

- `README.md` には現在の CLI 引数、実行方法、主要な使い方を保つこと。
- CLI の挙動や引数、既定値、実行例が変わったら、対応する `README.md` の記述も同じ変更で更新すること。
- コードを変更したら、毎回必ず `README.md` の更新が必要かどうかを確認すること。特に以下のいずれかに該当する変更があったときは漏らさず更新する:
  - CLI 引数、既定値、サブコマンド、integrator の追加・削除・名称変更
  - シーンの追加・削除・番号の入れ替え、シーン内容（配置・マテリアル・roughness 等のパラメータ）の変更
  - マテリアルや BSDF の追加・削除・挙動変更で、シーン表や実行例に影響があるもの
  - 実行例コマンドに使う `--spp`／`--depth`／`-i` の推奨値が変わるような変更
  - 依存クレートの追加・削除・メジャーバージョン更新
- 上記に該当しない純粋な内部リファクタやテスト追加のみの場合は、`README.md` の更新は不要と明示的に判断した上でスキップしてよい。

# Git 運用

- `git push` はこのリポジトリではユーザーの許可が必要。ユーザーの許可無しでの作業はローカルでのコミットやブランチ操作までに留めること。
- コミットメッセージは英語で簡潔に 1 行で済ませること。本文や冗長な説明は付けない。
